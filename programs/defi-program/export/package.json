{
  "name": "on-chain-operator-export",
  "private": true,
  "description": "Export the On-Chain Operator Program master file to Word and PDF",
  "scripts": {
    "build": "node render_mermaid.js ../ON-CHAIN-OPERATOR-PROGRAM.md && node build_docx.js ../ON-CHAIN-OPERATOR-PROGRAM.md ../On-Chain-Operator-Program.docx && node build_pdf.js ../ON-CHAIN-OPERATOR-PROGRAM.md ../On-Chain-Operator-Program.pdf"
  },
  "dependencies": {
    "docx": "^9.0.0",
    "marked": "^18.0.0",
    "mermaid": "^11.0.0",
    "playwright-core": "^1.50.0"
  }
}
