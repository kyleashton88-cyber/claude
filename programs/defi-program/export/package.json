{
  "name": "on-chain-operator-export",
  "private": true,
  "description": "Export the On-Chain Operator Program master file to Word and PDF",
  "scripts": {
    "images": "node build_images.js",
    "build": "node build_images.js && node render_mermaid.js ../ON-CHAIN-OPERATOR-PROGRAM.md && node build_docx.js ../ON-CHAIN-OPERATOR-PROGRAM.md ../On-Chain-Operator-Program.docx && node build_pdf.js ../ON-CHAIN-OPERATOR-PROGRAM.md ../On-Chain-Operator-Program.pdf"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "docx": "^9.0.0",
    "marked": "^18.0.0",
    "mermaid": "^11.0.0",
    "playwright-core": "^1.50.0"
  }
}
